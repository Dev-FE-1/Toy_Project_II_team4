import styled from 'styled-components';
import { CardBox } from '../../../components/cardBox/CardBox.style';

export const CalendarCardWrapper = styled(CardBox)`
  display: flex;
  height: 19.5rem;
  overflow: hidden;
  background-color: var(--color-black);
  margin: 1rem 0;
  border-radius: 30px;
  color: var(--color-white);
`;
